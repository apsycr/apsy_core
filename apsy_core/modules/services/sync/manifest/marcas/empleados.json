{
    "tabla": "empleados",
    "version": 3,

    "create_table": "CREATE TABLE IF NOT EXISTS empleados (id INTEGER PRIMARY KEY, nombre TEXT, cedula TEXT, fecha_nacimiento DATE)",

    "server_side": {

        "pull": {

            "sql": "SELECT id, nombre, cedula, fecha_nacimiento FROM empleados WHERE idsucursal = %s AND id > %s AND id <= @@last_id ORDER BY id",

            "args": [
                "idsucursal",
                "ultimo_id"
            ],

            "lookup": "JOIN sync_ids b ON b.id_servidor = ? AND b.tabla = 'empleados'",

            "update": {
                "set": "SET a.nombre = ?, a.cedula = ?, a.fecha_nacimiento = ?"
            }
        },

        "push": {
            "enabled": 0
        },

        "queue": {

            "sql": "SELECT id, nombre, cedula, fecha_nacimiento FROM empleados WHERE idsucursal = %s",

            "args": [
                "idsucursal"
            ]
        }
    },

    "client_side": {

        "version": 6

    }
}